name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: shopstic/deno:1.7.1
    steps:
      - uses: actions/checkout@v2
      - run: ./ci.sh
